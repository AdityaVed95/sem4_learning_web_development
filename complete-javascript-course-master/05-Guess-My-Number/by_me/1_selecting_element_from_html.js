'use strict'

document.querySelector('.message') // querySelector() is a method
// available on the document object 
// we want to select an element with the class name of : message
// if we had to select an id instead of a class then we would have 
// used #message instead of .message    